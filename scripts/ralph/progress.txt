# Ralph Progress Log
Started: $(date)
---

## Codebase Patterns
- **Backend path:** `/home/don/projects/ev-pulse/backend/` — add to Python path before importing
- **DB:** SQLite at `data/ev_pulse.db`, auto-seeded from `backend/app/seed_data.json`
- **Scoring:** 4-factor (0-100): keyword match (0-40) + market impact (0-30) + recency (0-20) + source authority (0-10)
- **Categories:** government_policy > ma_partnership > charger_install > charging_standards > grid_pricing > ev_sales_stats
- **Delivery:** No-agent cron scripts at `~/.hermes/scripts/ev-pulse-weekly.py` and `ev-pulse-monthly.py`
- **Weekly format:** TL;DR (top 3) → per-market with flag emojis → "why it matters" style → inline links
- **LLM:** DeepSeek API via raw urllib (no openai library on Render)
- **Frontend:** React+Vite at `frontend/`, WSJ/FT editorial style
- **Cron:** Both monthly and weekly run as no-agent scripts via Hermes cron
- **Venv:** `~/projects/ev-pulse/venv/`
- **why_it_matters:** Must be in DB schema (`why_it_matters TEXT DEFAULT ''`) + INSERT statement. Set by both `_fallback_analysis` (keyword-only) and `llm_analyze_article` (LLM mode).
- **DB migration:** New columns added in `init_db()` migration loop; also need manual `ALTER TABLE` on existing DB files


## [2026-07-01 20:30 BST] - US-001
- Added why_it_matters generation to _fallback_analysis in analyzer.py (category + region templates)
- Added why_it_matters column to DB schema in db.py
- Updated weekly digest fallback text in ev-pulse-weekly.py
- **Learnings:**
  - DB schema changes need both migration code (init_db) AND manual ALTER TABLE on existing DBs
  - The =*** placeholder in write_file content causes syntax errors — must write raw Python dicts
  - Category-specific fallback text is much more readable than generic "Key development"
---

## [2026-07-05] - US-006: Fix frontend category filters + wire graph toggles
- Fixed ArticlesPage category filter buttons: replaced hardcoded old category names ('service', 'trend', 'policy') with dynamic rendering from CATEGORIES map, matching actual backend categories
- Fixed GraphPage toggles: "Cats" and "Links" checkboxes now have onChange handlers and actually filter category nodes / article↔article backlinks from the graph
- Files changed: frontend/src/App.tsx
- **Learnings:**
  - The category filter buttons were still using pre-refactor values ('service', 'trend', 'policy') — must use Object.entries(CATEGORIES) for dynamic rendering
  - Graph toggles had checked state + state vars but no onChange handlers, making them purely cosmetic
  - filterRenderData pattern (useMemo on top of localGraph || graphData) cleanly layers toggle filters without redundant API calls
---

## [2026-07-12] - Search bar: frontend keyword search for ArticlesPage
- Added search input with 400ms debounce to ArticlesPage
- Wired to existing `/api/articles/search` endpoint (previously had no frontend UI)
- Search integrates with existing region/category/relevance filters
- CSV export now includes search query when present
- Context-aware empty state shows search terms when no results found
- Added `.search-bar`, `.search-input`, `.search-clear` CSS styles matching editorial design
- Files changed: frontend/src/App.tsx, frontend/src/index.css
- **Learnings:**
  - The search endpoint existed but had no frontend input — endpoint-first design is good but needs UI parity check
  - Debounce pattern (400ms) prevents excessive API calls during typing
  - Need to clear `debouncedQuery` too when clearing search input, not just `searchQuery`
---

## [2026-07-19] - Weekly maintenance: Fix missing route on reanalyze endpoint
- `reanalyze_all()` function in main.py had no `@app.post` decorator — was dead code despite being fully implemented
- Added `@app.post("/api/reanalyze")` to register the reanalyze endpoint
- Route referenced by `scripts/translate_articles.py` but previously returned 404
- Files changed: backend/app/main.py (1 line added)
- **Learnings:**
  - Always check that function definitions have their route decorators — a missing decorator silently produces dead code
  - The function was fully written (67 lines) but unreachable; `scripts/translate_articles.py` referenced it
---
